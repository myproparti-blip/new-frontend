import React from 'react';

const PrintPreview = ({ formData, imagePreviews, bankImagePreview }) => {
    return (
        <div className="print-container" style={{ fontFamily: 'Arial, sans-serif', backgroundColor: '#fff', padding: 0, margin: 0 }}>
        </div>
    );
};

export default PrintPreview;
